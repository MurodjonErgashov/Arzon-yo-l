<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    

  <!-- slider stylesheet -->
  <link rel="stylesheet" type="text/css" href="https://cdnjs.cloudflare.com/ajax/libs/OwlCarousel2/2.3.4/assets/owl.carousel.min.css" />

  <!-- bootstrap core css -->
  <link rel="stylesheet" type="text/css" href="/css/bootstrap.css" />

  <!-- fonts style -->
  <link href="https://fonts.googleapis.com/css?family=Poppins:400,700&display=swap" rel="stylesheet">

  <!-- Custom styles for this template -->
  <link href="/css/style.css" rel="stylesheet" />
  <!-- responsive style -->
  <link href="/css/responsive.css" rel="stylesheet" />
  <style>
    #totalPrice:hover{
        background-color: #c1c1c1;
        border-radius: 5px;
    }
  </style>
</head>
<body>
      <!-- <div class="hero_area"> -->
        <%- include("client_header") %>
      
      <div class="row">
        <div class="col-md-6 col-sm-12 m-auto">


            <div class="card">
                <form class="card-body" method="post" action="/client/orderroute">
                    <h2 class="text-center"><%= String(routes[0].kun).substring(0,16) %></h2>
                    <div class="p-3" style="border-left: 3px solid #777;">
                        <h4>Qayerdan:  </h4>
                        <p> <%= routes[0].address_qayerdan  %></p>
                        <h4>Qayergacha:  </h4>
                        <p> <%= routes[0].address_qayerga  %></p>
                        <a href="#" id="totalPrice" class="btn btn-outline border-bottom py-3 border-top w-100 d-flex justify-content-between">  <%= routes[0].ismi %><img src="dd" alt="dd"><!--span class="badge badge-secondary"> Cobalt Oq  T420TA</span--> </a>
                        <a href="#" id="totalPrice" class="btn btn-outline border-bottom py-3 border-top w-100 d-flex justify-content-between"> Mashina turi: <span class="badge badge-secondary"><%= routes[0].type_car %></span> </a>
                        <a href="#" id="totalPrice" class="btn btn-outline border-bottom py-3 border-top w-100 d-flex justify-content-between"> Umumiy summa <span class="badge badge-secondary"> <%= routes[0].summa %></span> </a>
                        <a href="#" id="totalPrice" class="btn btn-outline border-bottom py-3 border-top w-100 d-flex justify-content-between"> Chekish mumkinmi <span class="badge badge-secondary"><%= routes[0].has_chekish==1? 'Ha': 'Yo`q'%></span> </a>
                        <a href="#" id="totalPrice" class="btn btn-outline border-bottom py-3 border-top w-100 d-flex justify-content-between"> Uy hayvonlari chiqish mumkinmi <span class="badge badge-secondary"> <%= routes[0].has_uy_hayvoni==1? 'Ha': 'Yo`q'%></span> </a>
                        <a href="#" id="totalPrice" class="btn btn-outline border-bottom py-3 border-top w-100 d-flex justify-content-between"> Yaxna ichimliklar berladimi <span class="badge badge-secondary"><%= routes[0].has_snakes==1? 'Ha': 'Yo`q'%></span> </a>
                        <a href="#" id="totalPrice" class="btn btn-outline border-bottom py-3 border-top w-100 d-flex justify-content-between"> Haydovchi jinsi <span class="badge badge-secondary"> <%= routes[0].jinsi == 1? 'Erkak': 'Ayol' %></span> </a>
                        <input type="text" name="number_people" id="" value="<%= number_people %>" hidden>
                        <input type="text" name="route_id" id="" value="<%= route_id %>" hidden>
                        <input type="text" name="narxi" id="" value="<%= narxi %>" hidden>
                      </div>
                      <button type="submit" class="btn btn-secondary">Order</button>
                    </form>
            </div>
          
        </div>
      </div>
</body>
</html>